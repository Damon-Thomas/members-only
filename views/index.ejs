<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./css/home.css">
    <title>Members Only</title>
  </head>
  <body>
    <div class="header">
      <h1 class="title">Home</h1>
      <% if (locals.currentUser) { if (locals.currentUser.member) {%>
      <h3 class="accountType">Member Account</h3>
      <% }else{ %>
      <h3 class="accountType">Non-Member Account</h3>
      <% }} %>
    </div>
    <%- include('nav') %> 
    <% if (locals.currentUser) {%>
    <div class="midContent">
      <h1 class="welcomeMessage">
        WELCOME BACK <%= locals.currentUser.firstname + ' ' +
        locals.currentUser.lastname %>
      </h1>
      <a class="logOut" href="/log-out">LOG OUT</a>
    </div>
    <%}%>

    <%- include('message', {messages:messages}) %>
  </body>
</html>
